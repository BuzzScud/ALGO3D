# Cache files
cache/*.json
!cache/.gitkeep

# Database
*.sql
*.sqlite
*.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
error_log

# Environment
.env
config.local.php

# Sensitive config (contains API keys)
includes/config.php

# Data directory (contains database)
data/
*.db
*.sqlite

# Node.js dependencies
node_modules/

# Build outputs (keep source, ignore built files if desired)
# Note: tailwind.css is built from tailwind-input.css
# Uncomment the line below if you want to ignore built CSS:
# assets/css/tailwind.css

# Build directory (config files are tracked, but you can ignore build artifacts here)
# build/artifacts/
# build/dist/

# Build outputs (keep source, ignore built files)
# Note: tailwind.css is built from tailwind-input.css, but we'll track it for now
# Uncomment the line below if you want to ignore built CSS:
# assets/css/tailwind.css

